<div class="card">
    <h3>{{name}}</h3>
    <div class="card-img-top">
        <img src="{{flag}}" alt="{{name}}">
    </div>
    <div class="card-body">
        <p class="card-title">Capital: {{capital}}</p>
        <p class="card-text">Population: {{population}}</p>

        <p class="card-text"><b>Languages:</b></p>
        <ul class="list-group">
            {{#each languages}}
            <li class="list-group-item">{{name}}</li>
            {{/each}}
        </ul>
    </div>
</div>